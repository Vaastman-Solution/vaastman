model College {
  id     String   @id @default(cuid())
  name   String
  code 	 String?
  fees   Decimal
  domains Domain[]
}

model Domain {
  id       String    @id @default(cuid())
  name     String
  colleges College[]
}