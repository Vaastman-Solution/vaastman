model College {
  id     String   @id @default(cuid())
  name   String
  code 	 String?
  fees   Float
  domains Domain[]
}

model Domain {
  id       String    @id @default(cuid())
  name     String
  colleges College[]
}