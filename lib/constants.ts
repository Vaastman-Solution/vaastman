/**
 * Height of the public-facing Navbar1 in pixels (py-4 padding + ~40px content).
 * Stored as a plain number so you can do arithmetic: NAVBAR_HEIGHT * 2, etc.
 *
 * Pass it directly to <LoaderScreen offsetHeight={NAVBAR_HEIGHT} />
 * or multiply:        <LoaderScreen offsetHeight={NAVBAR_HEIGHT * 2} />
 */
export const NAVBAR_HEIGHT = 72; // px
